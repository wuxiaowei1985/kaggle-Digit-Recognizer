# 数据集（通常很大，且 Kaggle 上已有）
/data/

# 模型权重文件（二进制大文件，不适合 Git 管理）
/run/savemodel/best_model.pth
/run/savemodel/submission.csv
/weights/

# PyCharm 项目配置（每个人的本地配置不同）
.idea/

# Python 虚拟环境（通常不提交）
venv/
env/

# Python 缓存文件
__pycache__/
*.pyc